package org.synsystems.onlypass.components.cryptography.credentials;

/**
 * A credential where recovering the backing secret is not feasible.
 */
public interface SecureCredential extends Credential {}